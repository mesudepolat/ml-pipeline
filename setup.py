from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Correlations between calories burnt, exercise and body type',
    author='Mesude',
    license='',
)
